{% extends 'base.html.twig' %}

{% block title %}Jewelry index{% endblock %}

{% block body %}
    <h1 class="blue-grey-text center-align" >Les différents Bijoux</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Image</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Modifier</th>
                <th>Supprimer</th>
            </tr>
        </thead>
        <tbody>
        {% for jewelry in jewelries %}
            <tr>
                <td>{{ jewelry.id }}</td>
                <td>{{ jewelry.name }}</td>
                <td><img class="responsive-img" width="20%" src="{{ jewelry.picture }}" alt="{{ jewelry.name }}"></td>
                <td>{{ jewelry.description }}</td>
                <td>{{ jewelry.price }}€</td>

                <td>
                    <a href="{{ path('jewelry_show', {'id': jewelry.id}) }}"><i class="material-icons">visibility</i></a>

                </td>
                <td>
                    <a href="{{ path('jewelry_edit', {'id': jewelry.id}) }}"><i class="material-icons">edit</i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('jewelry_new') }}">Create new</a>
{% endblock %}
