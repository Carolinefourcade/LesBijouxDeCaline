<form method="post" action="{{ path('jewelry_delete', {'id': jewelry.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cet article?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jewelry.id) }}">
    <button class="btn blue">Supprimer</button>
</form>
